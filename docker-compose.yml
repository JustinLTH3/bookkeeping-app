services:
  postgres-test:
    image: postgres:16
    environment:
      POSTGRES_USER: bookkeeping
      POSTGRES_PASSWORD: bookkeeping
      POSTGRES_DB: bookkeeping_test
    ports:
      - "5433:5432"
